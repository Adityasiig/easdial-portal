// Removed in the auth refactor: EasDial no longer keeps a local user/password
// store. Authentication is delegated to Peeredge (see authService + sessionStore).
// This file is intentionally empty and can be deleted (`git rm`).
export {};
